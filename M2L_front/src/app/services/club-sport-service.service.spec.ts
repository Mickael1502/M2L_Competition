import { ClubSportService } from './club-sport-service.service';

describe('ClubSportService', () => {
  it('should create an instance', () => {
    expect(new ClubSportService()).toBeTruthy();
  });
});
